import { Placeholder } from "@/components/shell/Placeholder";

export default function ComparisonsPage() {
  return <Placeholder title="Comparisons" note="Comparison results & regression signals — wired to the live API in B.3." />;
}
