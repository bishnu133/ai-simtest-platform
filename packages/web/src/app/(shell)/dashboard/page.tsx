import { Placeholder } from "@/components/shell/Placeholder";

export default function DashboardPage() {
  return <Placeholder title="Dashboard" note="Demo fixture data — live dashboard persistence pending (B.4)." />;
}
