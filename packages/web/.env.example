# Server-side only — never expose these as NEXT_PUBLIC_*

# AI SimTest engine (simtest serve) — powers the simulation wizard at /
ENGINE_API_URL=http://127.0.0.1:8100

# Platform API (packages/api) — powers the /overview, /conversations, /comparisons shell pages
API_INTERNAL_URL=http://127.0.0.1:8000
# Optional dev-auth overrides (placeholders, not secrets)
# DEV_AUTH_BEARER=dev
# DEV_AUTH_USER_ID=dev_actor_1
