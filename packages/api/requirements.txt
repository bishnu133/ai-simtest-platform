# AI SimTest API — Week 5 + Postgres/Auth Wiring Sprint dependencies
# Install: pip install -r requirements.txt

# Runtime
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.1
httpx>=0.27
boto3>=1.34
botocore>=1.34,<1.40
python-multipart>=0.0.9

# Postgres + Auth Wiring Sprint (Turn 1)
sqlalchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29
alembic>=1.13
greenlet>=3.0

# Postgres + Auth Wiring Sprint (Turn 3) — JWT verification for Clerk
# and future OIDC providers. [crypto] extra pulls in the `cryptography`
# backend needed for RS256/ES256 signature verification.
PyJWT[crypto]>=2.8

# Development & test
pytest>=8.0
pytest-asyncio>=0.23
moto[s3]>=5.0
aiosqlite>=0.20

# Internal proprietary engine (headless core) — lean [core] extra only.
# NOTE: relative path assumes pip runs from packages/api (as `make install` does).
-e ../engine[core]
